# assets

把 Source 图标、App 图标和截图放在这里，再通过 GitHub Pages 地址引用。

建议：

- 图标使用 PNG 或 JPG，App 图标建议准备 1024×1024 的原图。
- 截图使用 PNG 或 JPG，并在 `apps.json` 的 `screenshots` 数组中填入公开 URL。
- `apps.json` 中的示例 URL 预期文件名为 `source-icon.png` 和 `app-icon.png`；使用自己的文件名时同步修改 JSON。
- 不要把 IPA 放在这里；IPA 应上传到 GitHub Releases。
